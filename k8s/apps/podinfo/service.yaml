apiVersion: v1
kind: Service
metadata:
  name: podinfo
  namespace: demo
spec:
  selector:
    app: podinfo
  ports:
    - port: 9898
      targetPort: 9898
  type: ClusterIP # no public LB — reach it via `kubectl port-forward`
