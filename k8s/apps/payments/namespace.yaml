apiVersion: v1
kind: Namespace
metadata:
  name: payments
  labels:
    app.kubernetes.io/part-of: payments-platform
    # kubernetes.io/metadata.name is auto-set to "payments" — the NetworkPolicies
    # below select kube-system by that same auto-label for DNS egress.
